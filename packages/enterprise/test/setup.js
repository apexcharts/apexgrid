// Loaded by `testRunnerHtml` ahead of the test framework so the patch is in
// place before any assertion runs. Shared with the other workspace packages;
// see the module for why it exists.
import '../../../test-setup/dom-safe-assertions.js';
